import React from "react";

const SlideTech = () => {
  return (
    <div
      style={{
        height: "88px",
        backgroundColor: "var(--text-color)",
        color: "var(--text-color)",
      }}
    ></div>
  );
};

export default SlideTech;
